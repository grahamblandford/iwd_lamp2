#/appspec.yml

version: 0.0

os: linux
files:
  - source: /
    destination: /var/www/html/iwd_lamp2/
    overwrite : yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu