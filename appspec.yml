#/appspec.yml

version: 0.0

os: linux
files:
  - source: /
    destination: /var/www/html/iwd_lamp2/
    file_exists_behavior: OVERWRITE
    overwrite : yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu